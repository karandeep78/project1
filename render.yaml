services:
  - type: web
    name: phish-detector
    runtime: python
    plan: free
    rootDir: Phishing-detector
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
