services:
  - type: web
    name: phish-detector
    rootDir: .
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r ./requirements.txt
    startCommand: gunicorn app:app
